COURSE表
CID|CNAME|TID
001|企业管理|9001
002|马列|9002
003|UML|9006
004|数据库|9004
006|英语|9006

TEACHER表
TID|TNAME
9001|崔老师
9002|颜老师
9003|岳老师
9004|夏老师
9005|杨老师
9006|叶老师
9007|梦老师

SC表
SID|CID|SCORE

STUDENT表
SID|SNAME|SAGE|SSEX

JOIN == , == CROSS JOIN == FULL JOIN
CROSS JOIN返回两表的数据行的乘积的记录
一下四行返回结果一样
SELECT * FROM TABLE_A CROSS JOIN TABLE_B
SELECT * FROM TABLE_A FULL JOIN TABLE_B
SELECT * FROM TABLE_A JOIN TABLE_B
SELECT * FROM TABLE_A, TABLE_B

OUT JOIN 返回一张表的所有行，并带有来自另外一张表的条件相符的行
LEFT OUTER JOIN == LEFT JOIN
RIGHT OUTER JOIN == RIGHT JOIN

INNER JOIN 两表都存在的数据，要联合条件查询

UNION合并几个结果集，并且排除重复数据
UNION合并几个结果集，但不排除重复数据

GROUP BY一般和聚合函数SUM/AVG一起使用
SELECT CID, AVG(SCORE) FROM SCORE AS S GROUP BY S.CID;
SELECT 
	C.CNAME, AVG(S.SCORE) 
FROM 
	SCORE AS S 
INNER JOIN 
	COURSE AS C 
ON 
	C.CID=S.CID 
GROUP BY 
	C.CID;
	
IN和EXISTS
一下两句结果集相同
SELECT TNAME FROM TEACHER WHERE TID IN (SELECT TID FROM COURSE);
SELECT TNAME FROM TEACHER WHERE EXISTS (SELECT * FROM COURSE WHERE COURSE.TID = TEACHER.TID);